---
upgrade:
  - |
    :func:`~circuit_knitting.cutting.execute_experiments` will no longer create separate Qiskit Runtime jobs for each subcircuit by default.
    Now, separate jobs will only be created if separate :class:`~qiskit.primitives.BaseSampler` instances are provided for each circuit partition.
